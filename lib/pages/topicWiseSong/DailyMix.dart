import 'package:flutter/material.dart';

class DailMix extends StatelessWidget {
  const DailMix({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
body: Center(child: Text("Daily Mix"),),
    );
  }
}
